{
  "name": "company-pro-bun",
  "main": "index.ts",
  "module": "index.ts",
  "private": true,
  "scripts": {
    "dev": "bun --hot run server.ts",
    "start": "bun run server.ts",
    "build": "esbuild api/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "vercel-build": "esbuild api/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.5"
  }
}
